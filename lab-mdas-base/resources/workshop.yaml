apiVersion: training.educates.dev/v1beta1
kind: Workshop
metadata:
  name: lab-mdas-base
spec:
  title: Starter Workshop
  description: Starter workshop for Educates
  workshop:
    files:
    - image:
        url: $(image_repository)/lab-mdas-base-files:latest
      includePaths:
      - /workshop/**
      - /templates/**
      - /README.md
    packages:
    - name: gemfire
      files:
      - image:
          url: ghcr.io/morgan-iverson/mdas-packages/gemfire:sha-a900553
  session:
    namespaces:
      budget: medium
    applications:
      terminal:
        enabled: true
        # layout: split
      console:
        enabled: true
      editor:
        enabled: true
    dashboards: 
    - name: Terminal 2
      url: terminal:terminal-2